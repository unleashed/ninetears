

inherit "/std/item";

void setup()
{
    set_name("ramo");
    set_short("Ramo de Flores");
    set_weight(10);
    set_genero(0);
    add_alias("flores");
    set_main_plural("Ramos de Flores");
    add_plural("ramos");
    set_long("Se trata de un ramo de flores de la Floristería de Noella. "
     "Te preguntas si debía o no tener targeta de felicitación.\n");
}
