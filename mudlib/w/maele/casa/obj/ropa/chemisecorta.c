inherit "/obj/armour";
void setup() {
   	set_base_armour("pechera");
   	set_name("Chemise de mangas cortas");
   	set_short("Chemise de mangas cortas");
   	add_alias("chemise");
   	add_plural("Chemises");
   	set_main_plural("Chemises");
   	set_long(query_short()+"\n Es una camisa suelta que cubre el cuerpo desde el cuello hasta el muslo, cubre tambien los brazos parcialmente. Su bordado es bastante rustico y es usada principalmente por gente de baja clase social.\n");
   	set_weight(5);
   	}