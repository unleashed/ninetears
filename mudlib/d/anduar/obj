object quien;

void init_shadow(object ob) 
{
  quien = ob;
  call_out("comprobar",8);
}

int comprobar()
{
if (!quien->query_property("conmasdos"))
	{
	TP->adjust_tmp_dex(-3,30*60);
	destruct(TO);
	}
call_out("comprobar",8);
}
